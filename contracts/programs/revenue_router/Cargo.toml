[package]
name = "revenue-router"
version = "0.1.0"
description = "Revenue Router - Classifies and distributes USDC from sales"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "revenue_router"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.29.0"
anchor-spl = "0.29.0"
